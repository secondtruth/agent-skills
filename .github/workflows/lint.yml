name: lint
on:
  pull_request:
  push:
    branches: [main]
jobs:
  skills:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint skills against the writing-for-agents rubric
        run: scripts/lint-skills.sh --public --strict
